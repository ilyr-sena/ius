import SwiftUI

@main
struct ProbeTargetApp: App {
    var body: some Scene {
        WindowGroup {
            Text("IUS probe — this target app is unused. The Runner hosts the probe.")
        }
    }
}